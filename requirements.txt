# FastAPI y dependencias
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic[email]==2.5.0
python-multipart==0.0.6

# Firebase
firebase-admin==6.3.0

# AWS S3 (para almacenamiento de fotos DAGMA)
boto3==1.34.0

# Prometheus para métricas
prometheus-client==0.19.0

# Rate limiting
slowapi==0.1.9

# Geolocalización
numpy<2
shapely==2.0.2

# Whisper local (transcripción de audio gratuita)
faster-whisper>=1.0.0

# Clasificador local de centros gestores (SLM gratuito multilingüe)
# Rama de reglas léxicas funciona sin estas libs; rama de embeddings las requiere.
sentence-transformers>=2.7.0
# torch CPU (Railway / Linux x86_64). En local Windows, pip resolverá la wheel CPU adecuada.
torch>=2.1.0,<3

# Generación de PDF
reportlab==4.2.5
Pillow>=10.0.0

# Utilidades
python-dotenv==1.0.0
httpx==0.27.0

# Web Push (notificaciones iOS PWA + Android + desktop)
pywebpush==2.0.0
py-vapid==1.9.1
